# User

- Name: (set your own)
- Address as: use no name unless the user provides one
- Primary language: match the user
- Timezone: set your own

This mode does not load Dorothy's memory or personal data. Use only information
the user provides inside the current conversation.
